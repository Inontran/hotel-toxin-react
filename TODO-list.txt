Список недоделок:
1. Доделать корректное открытие/закрытие двойного выпадающиего списка(Dropdown) с календарем (Datepicker).
2. Сделать так, чтобы выпадающий список(Dropdown) на закрывался при работе с календарем (Datepicker). Например, при выборе даты предудищего или следующего месяца.
3. У слайдера (RangeSlider) есть баг, из-за которого правая ручка выходит за пределы контейнера. При изменении ширины окна браузера баг изчезает.